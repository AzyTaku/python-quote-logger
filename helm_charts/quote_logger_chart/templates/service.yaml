apiVersion: v1
kind: Service
metadata:
  name: {{ include "quote_logger_chart.fullname" . }}
  labels:
    {{- include "quote_logger_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quote_logger_chart.selectorLabels" . | nindent 4 }}
