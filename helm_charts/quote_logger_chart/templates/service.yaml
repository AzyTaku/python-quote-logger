apiVersion: v1
kind: Service
metadata:
  name: {{ include "quote-logger-chart.fullname" . }}
  labels:
    {{- include "quote-logger-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quote-logger-chart.selectorLabels" . | nindent 4 }}
