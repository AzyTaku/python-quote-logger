apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quote_logger_chart.fullname" . }}-test-connection"
  labels:
    {{- include "quote_logger_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quote_logger_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
