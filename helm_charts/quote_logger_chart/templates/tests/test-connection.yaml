apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quote-logger-chart.fullname" . }}-test-connection"
  labels:
    {{- include "quote-logger-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quote-logger-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
